# User-local Zsh plugin extensions.
#
# This file is sourced after the managed defaults
# (home/dev/config/zsh/default/plugins.zsh), which bootstraps Zap and
# declares the standard plugins.
#
# Add extra `plug` declarations below as needed.
